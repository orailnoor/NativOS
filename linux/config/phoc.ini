[output]
# phoc compositor configuration for NativOS
# This runs nested inside LorieView's X11 server

# Scale factor for phone-sized screens
# 2.0 = HiDPI (good for 1080p+ phone screens)
scale = 2

# For nested mode on X11 (Option B)
# phoc will open an X11 window on LorieView's display
[output:X11-1]
scale = 2

# Rotation: 0, 90, 180, 270
# Default to portrait for phone
# transform = 0
